FROM rust:1-bookworm AS build
WORKDIR /src
COPY . .
RUN cargo build --release -p pharus-agent

FROM debian:bookworm-slim
RUN useradd -r -u 10001 pharus
COPY --from=build /src/target/release/pharus-agent /usr/local/bin/pharus-agent
USER pharus
ENTRYPOINT ["pharus-agent"]
